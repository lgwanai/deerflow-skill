---
phase: 01-core-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - pyproject.toml
  - lib/__init__.py
  - README.md
autonomous: true
requirements:
  - CORE-01
  - CONF-04

must_haves:
  truths:
    - "User can see the skill definition in SKILL.md"
    - "User can install dependencies via pyproject.toml"
    - "Skill imports deerflow-harness package successfully"
  artifacts:
    - path: "SKILL.md"
      provides: "Claude Code skill definition"
      contains: "---\nname: deer-flow"
    - path: "pyproject.toml"
      provides: "Python project configuration with deerflow-harness dependency"
      contains: "deerflow-harness"
    - path: "lib/__init__.py"
      provides: "Python package marker"
      min_lines: 1
  key_links:
    - from: "pyproject.toml"
      to: "deerflow-harness"
      via: "dependencies list"
      pattern: "deerflow-harness"
---

<objective>
Establish the core skill structure with Claude Code skill definition, Python project configuration, and deerflow-harness dependency declaration. This creates the foundation for the embedded agent integration.

Purpose: Skill must be recognizable by Claude Code and have proper dependency declaration before implementation work begins.
Output: SKILL.md, pyproject.toml, lib/__init__.py, README.md
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@.planning/phases/01-core-integration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md with Claude Code skill definition</name>
  <files>SKILL.md</files>
  <action>
    Create SKILL.md with YAML frontmatter defining the deer-flow skill.
    
    Required frontmatter fields:
    - name: deer-flow
    - description: "Invoke deer-flow agent orchestration directly in Claude Code. Use for complex tasks requiring multi-step reasoning, tool orchestration, or subagent delegation. Supports mode presets: --flash (fastest), --standard (default), --pro (with planning), --ultra (with subagents)."
    
    Body must include:
    - Overview section explaining embedded agent (no server)
    - Usage section with command examples for all mode presets
    - Configuration section noting config.yaml requirement
    - Note that config.example.yaml is auto-created if missing
    
    Pattern reference from RESEARCH.md Section "SKILL.md Format".
  </action>
  <verify>
    <automated>test -f SKILL.md && grep -q "name: deer-flow" SKILL.md && grep -q "description:" SKILL.md</automated>
  </verify>
  <done>SKILL.md exists with valid YAML frontmatter and deer-flow skill definition</done>
</task>

<task type="auto">
  <name>Task 2: Create pyproject.toml with deerflow-harness dependency</name>
  <files>pyproject.toml</files>
  <action>
    Create pyproject.toml declaring deerflow-harness as main dependency.
    
    Required sections:
    - [project]: name="deerflow-skill", version="0.1.0", requires-python=">=3.12"
    - dependencies: ["deerflow-harness>=0.1.0"]
    - [project.optional-dependencies] dev: pytest>=8.0.0, pytest-cov>=4.0.0
    - [build-system]: hatchling backend
    
    Pattern reference from RESEARCH.md Section "pyproject.toml".
  </action>
  <verify>
    <automated>test -f pyproject.toml && grep -q "deerflow-harness" pyproject.toml</automated>
  </verify>
  <done>pyproject.toml exists with deerflow-harness declared as dependency</done>
</task>

<task type="auto">
  <name>Task 3: Create lib package structure and README</name>
  <files>lib/__init__.py, README.md</files>
  <action>
    Create lib/__init__.py as empty package marker (can have docstring).
    
    Create README.md with:
    - Skill overview (embedded deer-flow agent)
    - Installation instructions (pip install or uv add)
    - Quick start with config.yaml setup
    - Mode presets documentation (flash, standard, pro, ultra)
    - Configuration reference
    
    README should help users get started quickly.
  </action>
  <verify>
    <automated>test -f lib/__init__.py && test -f README.md && grep -q "deer-flow" README.md</automated>
  </verify>
  <done>lib/__init__.py package marker and README.md documentation exist</done>
</task>

</tasks>

<verification>
- SKILL.md has valid YAML frontmatter with name and description
- pyproject.toml has deerflow-harness in dependencies
- lib/__init__.py exists as package marker
- README.md documents installation and usage
</verification>

<success_criteria>
- Claude Code can recognize skill from SKILL.md
- Dependencies can be installed via pyproject.toml
- Project structure follows CONTEXT.md decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-01-SUMMARY.md`
</output>