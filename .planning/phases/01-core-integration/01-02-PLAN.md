---
phase: 01-core-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/config.py
  - lib/errors.py
  - config.example.yaml
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CONF-01
  - CONF-02
  - CONF-03
  - LLM-04

must_haves:
  truths:
    - "User receives clear error when config.yaml is missing"
    - "User receives clear error when deerflow-harness is not installed"
    - "User receives clear error when model credentials are missing"
    - "Skill resolves config.yaml path via DEER_FLOW_CONFIG_PATH or default locations"
  artifacts:
    - path: "lib/config.py"
      provides: "Config path resolution and validation"
      exports: ["resolve_config_path", "validate_config", "resolve_and_validate_config"]
      min_lines: 50
    - path: "lib/errors.py"
      provides: "Error message formatting and templates"
      exports: ["format_error", "MISSING_PACKAGE_MSG", "MISSING_CONFIG_MSG", "MISSING_CREDENTIALS_MSG"]
      min_lines: 40
    - path: "config.example.yaml"
      provides: "Example config template for users"
      contains: "models:"
  key_links:
    - from: "lib/config.py"
      to: "config.yaml"
      via: "Path resolution"
      pattern: "DEER_FLOW_CONFIG_PATH"
    - from: "lib/errors.py"
      to: "stderr output"
      via: "format_error function"
      pattern: "format_error"
---

<objective>
Implement configuration loading with path resolution, validation, and actionable error messages. Users receive clear guidance when config is missing, package is missing, or credentials are not set.

Purpose: Clear error messages are critical for user success - bad errors frustrate users and block adoption.
Output: lib/config.py, lib/errors.py, config.example.yaml
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@.planning/phases/01-core-integration/01-RESEARCH.md
@.planning/phases/01-core-integration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement config path resolution in lib/config.py</name>
  <files>lib/config.py</files>
  <behavior>
    - Test 1: When DEER_FLOW_CONFIG_PATH is set and file exists, returns that path
    - Test 2: When DEER_FLOW_CONFIG_PATH is not set, checks ./config.yaml first
    - Test 3: When ./config.yaml missing, checks ../config.yaml
    - Test 4: When all paths fail, raises FileNotFoundError with clear message
    - Test 5: Calls create_example_config() when config missing
  </behavior>
  <action>
    Implement config path resolution matching CONTEXT.md decisions.
    
    Resolution order (exact):
    1. DEER_FLOW_CONFIG_PATH environment variable
    2. ./config.yaml (current working directory)
    3. ../config.yaml (parent directory)
    
    Functions to implement:
    - resolve_config_path() -> Path: Returns path or raises FileNotFoundError
    - create_example_config() -> None: Creates config.example.yaml template
    - validate_config(config_path: Path) -> None: Validates YAML parseable and credentials present
    - resolve_and_validate_config() -> Path: Combined convenience function
    
    Use AppConfig.from_file() for YAML parsing (from deerflow.config.app_config).
    Pattern reference from RESEARCH.md Section "Pattern 2: Config Path Resolution" and "Pitfall 3: Missing Credentials".
    
    For credential validation: check each model's api_key is non-empty and doesn't start with "$" (env var not expanded).
  </action>
  <verify>
    <automated>python -c "from lib.config import resolve_config_path, validate_config, resolve_and_validate_config" && echo "Imports OK"</automated>
  </verify>
  <done>lib/config.py implements path resolution with DEER_FLOW_CONFIG_PATH priority, validation, and creates example template on missing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement error message templates in lib/errors.py</name>
  <files>lib/errors.py</files>
  <behavior>
    - Test 1: format_error(ImportError) returns missing package message with pip/uv commands
    - Test 2: format_error(FileNotFoundError with config context) returns missing config message
    - Test 3: format_error with credential errors returns full guidance with env var names
    - Test 4: format_error with unknown error returns raw error message
  </behavior>
  <action>
    Implement error message formatting matching CONTEXT.md decisions exactly.
    
    Required message templates:
    - MISSING_PACKAGE_MSG: Shows pip install and uv add commands
    - MISSING_CONFIG_MSG: Shows that config.example.yaml was created
    - MISSING_CREDENTIALS_MSG: Shows missing env var names, example config.yaml, and shell export commands
    
    format_error(e: Exception) -> str function:
    - Match error type to appropriate template
    - For missing credentials: include specific env var names
    - For LLM provider errors: pass through raw message (no wrapping per CONTEXT.md)
    
    Pattern reference from RESEARCH.md Section "Pitfall 1", "Pitfall 2", "Pitfall 3".
  </action>
  <verify>
    <automated>python -c "from lib.errors import format_error, MISSING_PACKAGE_MSG, MISSING_CONFIG_MSG" && echo "Imports OK"</automated>
  </verify>
  <done>lib/errors.py provides actionable error messages for all failure modes</done>
</task>

<task type="auto">
  <name>Task 3: Create config.example.yaml template</name>
  <files>config.example.yaml</files>
  <action>
    Create config.example.yaml with example model configurations.
    
    Must include:
    - OpenAI model example (langchain_openai:ChatOpenAI)
    - Anthropic model example (langchain_anthropic:ChatAnthropic)
    - Ollama model example (optional, commented)
    - api_key using $ENV_VAR syntax
    - sandbox.enabled: false (skill runs embedded)
    
    Pattern reference from RESEARCH.md Section "Pitfall 2" example template.
  </action>
  <verify>
    <automated>test -f config.example.yaml && grep -q "langchain_openai" config.example.yaml && grep -q "langchain_anthropic" config.example.yaml</automated>
  </verify>
  <done>config.example.yaml exists with example configurations for all providers</done>
</task>

</tasks>

<verification>
- lib/config.py implements resolve_config_path, validate_config, resolve_and_validate_config
- lib/errors.py implements format_error with actionable messages
- config.example.yaml has OpenAI, Anthropic, and Ollama examples
</verification>

<success_criteria>
- Missing config produces clear error with example template created
- Missing package produces pip/uv install guidance
- Missing credentials produce full guidance with env var names
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-02-SUMMARY.md`
</output>