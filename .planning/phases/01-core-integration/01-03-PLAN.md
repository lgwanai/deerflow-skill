---
phase: 01-core-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - lib/modes.py
  - skill.py
  - scripts/chat.sh
  - tests/__init__.py
  - tests/test_config.py
  - tests/test_modes.py
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - LLM-01
  - LLM-02
  - LLM-03

must_haves:
  truths:
    - "User can invoke the skill and receive a response from the deer-flow agent"
    - "User can use any configured LLM provider (OpenAI, Anthropic, Ollama)"
    - "User can select mode presets (--flash, --standard, --pro, --ultra)"
  artifacts:
    - path: "lib/modes.py"
      provides: "Mode preset definitions and DeerFlowClient config mapping"
      exports: ["MODE_PRESETS", "get_mode_config"]
      min_lines: 30
    - path: "skill.py"
      provides: "Skill entry point with DeerFlowClient invocation"
      contains: "if __name__ == '__main__'"
      min_lines: 50
    - path: "scripts/chat.sh"
      provides: "Shell wrapper for quick usage"
      min_lines: 10
  key_links:
    - from: "skill.py"
      to: "DeerFlowClient"
      via: "from deerflow.client import DeerFlowClient"
      pattern: "DeerFlowClient"
    - from: "skill.py"
      to: "lib.config"
      via: "resolve_and_validate_config"
      pattern: "resolve_and_validate_config"
    - from: "skill.py"
      to: "lib.modes"
      via: "get_mode_config"
      pattern: "get_mode_config"
    - from: "skill.py"
      to: "lib.errors"
      via: "format_error"
      pattern: "format_error"
---

<objective>
Complete the skill implementation with mode presets, DeerFlowClient invocation, and test coverage. The skill becomes runnable and can invoke the agent with responses.

Purpose: Deliver working skill that users can invoke immediately after configuration.
Output: lib/modes.py, skill.py, scripts/chat.sh, tests/
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@.planning/phases/01-core-integration/01-RESEARCH.md
@.planning/phases/01-core-integration/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement mode presets in lib/modes.py</name>
  <files>lib/modes.py</files>
  <behavior>
    - Test 1: get_mode_config("flash") returns thinking_enabled=False, plan_mode=False, subagent_enabled=False
    - Test 2: get_mode_config("standard") returns thinking_enabled=True, plan_mode=False, subagent_enabled=False
    - Test 3: get_mode_config("pro") returns thinking_enabled=True, plan_mode=True, subagent_enabled=False
    - Test 4: get_mode_config("ultra") returns thinking_enabled=True, plan_mode=True, subagent_enabled=True
    - Test 5: get_mode_config("unknown") raises ValueError
  </behavior>
  <action>
    Implement mode preset mapping matching CONTEXT.md decisions exactly.
    
    Mode presets (exact values):
    - flash: thinking=false, plan_mode=false, subagent=false (fastest)
    - standard: thinking=true, plan_mode=false, subagent=false (default)
    - pro: thinking=true, plan_mode=true, subagent=false
    - ultra: thinking=true, plan_mode=true, subagent=true
    
    Use dataclass ModeConfig with thinking_enabled, plan_mode, subagent_enabled fields.
    MODE_PRESETS dict maps mode name to ModeConfig.
    get_mode_config(mode: str) -> dict returns kwargs for DeerFlowClient constructor.
    
    Pattern reference from RESEARCH.md Section "Pattern 3: Mode Preset Mapping".
  </action>
  <verify>
    <automated>python -c "from lib.modes import get_mode_config; c=get_mode_config('standard'); print(c)" | grep -q "thinking_enabled"</automated>
  </verify>
  <done>lib/modes.py implements all four mode presets with correct DeerFlowClient kwargs mapping</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement skill.py entry point</name>
  <files>skill.py</files>
  <behavior>
    - Test 1: skill.py with valid config and prompt returns agent response
    - Test 2: skill.py --flash "prompt" uses flash mode
    - Test 3: skill.py without prompt shows usage error
    - Test 4: skill.py with missing config shows clear error message
  </behavior>
  <action>
    Implement skill.py entry point matching CONTEXT.md decisions.
    
    Required components:
    - Import deerflow.client.DeerFlowClient with ImportError handling (shows install guidance)
    - Import lib.config (resolve_and_validate_config)
    - Import lib.modes (get_mode_config)
    - Import lib.errors (format_error)
    - parse_args(argv) -> tuple[str, str]: Returns (mode, prompt)
    - main(): Orchestrates config resolution, client creation, chat invocation
    
    CLI format: skill.py [--flash|--standard|--pro|--ultra] "prompt"
    Default mode: standard
    Thread ID: Auto-generate with uuid.uuid4() (stateless)
    
    Use client.chat(prompt) for one-shot response (not stream - deferred to Phase 2).
    
    Pattern reference from RESEARCH.md Section "Basic Skill Invocation (skill.py)".
  </action>
  <verify>
    <automated>python -c "import skill; print('Import OK')" 2>&1 | head -1</automated>
  </verify>
  <done>skill.py is runnable entry point that invokes DeerFlowClient with mode presets</done>
</task>

<task type="auto">
  <name>Task 3: Create shell wrapper and test scaffolds</name>
  <files>scripts/chat.sh, tests/__init__.py, tests/test_config.py, tests/test_modes.py</files>
  <action>
    Create scripts/chat.sh wrapper for shell usage:
    - Invokes skill.py with arguments passed through
    - Example: ./scripts/chat.sh "hello" or ./scripts/chat.sh --flash "quick task"
    
    Create tests/__init__.py as package marker.
    
    Create tests/test_config.py scaffold with test structure for:
    - test_path_resolution_env_var
    - test_path_resolution_cwd
    - test_path_resolution_parent
    - test_config_missing_creates_example
    - test_credentials_validation
    
    Create tests/test_modes.py scaffold with test structure for:
    - test_flash_mode
    - test_standard_mode
    - test_pro_mode
    - test_ultra_mode
    - test_unknown_mode_raises
    
    Test implementations can be placeholder assertions for Wave 0 verification.
  </action>
  <verify>
    <automated>test -f scripts/chat.sh && test -f tests/test_config.py && test -f tests/test_modes.py</automated>
  </verify>
  <done>Shell wrapper and test scaffolds exist</done>
</task>

</tasks>

<verification>
- lib/modes.py implements four mode presets
- skill.py invokes DeerFlowClient.chat() with mode settings
- scripts/chat.sh provides shell wrapper
- tests/ directory has test scaffolds
</verification>

<success_criteria>
- Skill can be invoked: `python skill.py "prompt"` returns agent response
- Mode presets work: `python skill.py --flash "prompt"` uses correct settings
- Multi-provider support: OpenAI, Anthropic, Ollama models configurable
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-03-SUMMARY.md`
</output>