---
phase: 02-streaming-and-error-handling
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_stream.py, tests/conftest.py, tests/test_errors.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test stubs exist for all STRM-* and ERRR-* requirements"
    - "Mock fixtures simulate DeerFlowClient.stream() behavior"
    - "Running pytest discovers all placeholder tests"
  artifacts:
    - path: "tests/test_stream.py"
      provides: "Test stubs for streaming module (STRM-01 to STRM-04)"
      contains: "test_.*stream.*"
    - path: "tests/conftest.py"
      provides: "Mock fixtures for DeerFlowClient and StreamEvent"
      exports: ["mock_deerflow_client", "mock_stream_event"]
    - path: "tests/test_errors.py"
      provides: "Extended test stubs for streaming errors (ERRR-01 to ERRR-04)"
      contains: "test_.*streaming.*|test_.*recursion.*"
  key_links:
    - from: "tests/conftest.py"
      to: "DeerFlowClient"
      via: "unittest.mock.Mock"
      pattern: "Mock(spec=['stream', 'chat'])"
---

<objective>
Create Wave 0 test infrastructure before Plan 02-01 executes.

Purpose: Tests must exist before implementation begins per TDD methodology.
Output: test_stream.py stubs, conftest.py mock fixtures, test_errors.py extensions.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-and-error-handling/02-VALIDATION.md

<!-- Phase 1 patterns to follow -->
@.planning/phases/01-core-integration/01-03-SUMMARY.md

<interfaces>
<!-- From deerflow-harness (reference) -->
From deerflow/client.py (external package):
```python
class StreamEvent:
    type: str  # "values", "messages-tuple", "custom", "end"
    data: dict

class DeerFlowClient:
    def stream(self, message: str, thread_id: str | None = None) -> Iterator[StreamEvent]
```

<!-- From existing lib/errors.py -->
From lib/errors.py:
```python
def format_error(e: Exception) -> str:
    """Format an exception into an actionable error message."""

MISSING_PACKAGE_MSG = "..."
MISSING_CONFIG_MSG = "..."
MISSING_CREDENTIALS_MSG = "..."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/conftest.py with mock fixtures</name>
  <files>tests/conftest.py</files>
  <action>
    Create tests/conftest.py with pytest fixtures for Phase 2 testing:

    1. mock_stream_event factory fixture:
       - Creates StreamEvent-like objects with type and data attributes
       - Factory function: `mock_stream_event(event_type: str, data: dict) -> Mock`
       - Supports all event types: "values", "messages-tuple", "custom", "end"

    2. mock_deerflow_client fixture:
       - Mocks DeerFlowClient class
       - Configurable stream() method that yields mock events
       - Has `spec=['stream', 'chat']` for proper attribute checking

    3. Pre-built scenario fixtures:
       - simple_text_stream: Events for basic text streaming
       - tool_call_stream: Events with tool call and result
       - error_stream: Events leading to an exception
       - retry_stream: Events with LLM retry custom events

    Use unittest.mock.Mock for all mocks. Include type hints and docstrings.
  </action>
  <verify>
    <automated>pytest tests/ --collect-only 2>&1 | grep -q "conftest.py"</automated>
  </verify>
  <done>conftest.py exists with all fixtures, `pytest --fixtures` shows them</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_stream.py with test stubs</name>
  <files>tests/test_stream.py</files>
  <action>
    Create tests/test_stream.py with placeholder test functions:

    1. Test class: TestStreamAndPrint
       - test_token_streaming: STRM-01 - tokens stream to stdout with flush=True
       - test_tool_progress: STRM-02 - tool call notifications appear
       - test_custom_events: STRM-03 - retry progress shown
       - test_end_event: STRM-04 - end event handled cleanly

    2. Test class: TestStreamingErrors (for ERRR-02)
       - test_tool_error_continues: Tool execution errors don't crash stream
       - test_partial_output_preserved: Output accumulated before error

    Each test should have:
    - `pytest.skip("Wave 0 stub - implementation pending")` as body
    - Clear docstring describing expected behavior
    - Reference to requirement ID (e.g., "STRM-01")

    This enables `pytest tests/ --collect-only` to discover all tests.
  </action>
  <verify>
    <automated>pytest tests/test_stream.py --collect-only 2>&1 | grep -c "test_" | grep -q "[5-9]"</automated>
  </verify>
  <done>test_stream.py exists with 6+ test stubs, all marked as skipped</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend tests/test_errors.py for streaming errors</name>
  <files>tests/test_errors.py</files>
  <action>
    Add streaming error test stubs to existing tests/test_errors.py:

    Add new test class: TestStreamingErrorMessages
    - test_recursion_limit_message: ERRR-01 - GraphRecursionError shows clear message
    - test_llm_timeout_message: ERRR-02 - timeout errors show guidance
    - test_llm_quota_message: ERRR-02 - quota errors show dashboard links
    - test_llm_auth_message: ERRR-02 - auth errors show API key guidance
    - test_stateless_session_info: ERRR-03 - session behavior documented
    - test_actionable_guidance: ERRR-04 - all messages are actionable

    Each test should have:
    - `pytest.skip("Wave 0 stub - implementation pending")` as body
    - Clear docstring describing expected behavior
    - Reference to requirement ID

    Keep existing test classes (TestFormatError, TestMessageTemplates, TestErrorDetection).
  </action>
  <verify>
    <automated>pytest tests/test_errors.py --collect-only 2>&1 | grep -c "test_" | grep -q "[1][0-9]"</automated>
  </verify>
  <done>test_errors.py extended with 6 new streaming error test stubs</done>
</task>

</tasks>

<verification>
- All test files collectible: `pytest tests/ --collect-only`
- conftest.py fixtures visible: `pytest --fixtures`
- Test count matches VALIDATION.md: at least 12 total test stubs
</verification>

<success_criteria>
- [ ] tests/conftest.py exists with mock_deerflow_client fixture
- [ ] tests/test_stream.py exists with 6+ test stubs
- [ ] tests/test_errors.py has 6+ additional streaming error test stubs
- [ ] `pytest tests/ --collect-only` discovers all placeholder tests
- [ ] VALIDATION.md wave_0_complete can be set to true after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-and-error-handling/02-00-SUMMARY.md`
</output>
