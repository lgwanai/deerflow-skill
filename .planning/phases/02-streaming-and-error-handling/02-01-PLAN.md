---
phase: 02-streaming-and-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/stream.py, tests/test_stream.py]
autonomous: true
requirements: [STRM-01, STRM-02, STRM-03, STRM-04]
user_setup: []

must_haves:
  truths:
    - "User sees agent responses stream token-by-token in real-time"
    - "User sees tool call notifications during agent runs"
    - "User sees tool completion notifications"
    - "User sees LLM retry progress when provider retries"
    - "Streaming handles errors gracefully without crashing"
  artifacts:
    - path: "lib/stream.py"
      provides: "Stream event handling and output formatting"
      exports: ["stream_and_print", "StreamEventHandler"]
    - path: "tests/test_stream.py"
      provides: "Test coverage for streaming behavior"
  key_links:
    - from: "lib/stream.py"
      to: "DeerFlowClient.stream()"
      via: "client.stream(message, thread_id=thread_id)"
      pattern: "for event in client.stream"
    - from: "lib/stream.py"
      to: "stdout"
      via: "print(content, end='', flush=True)"
      pattern: "flush=True"
---

<objective>
Implement token-by-token streaming for deer-flow agent responses with real-time progress feedback.

Purpose: Provide modern UX with real-time feedback so users can see agent progress instead of waiting for complete response.
Output: lib/stream.py module with stream_and_print function and StreamEventHandler class, plus comprehensive tests.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-and-error-handling/02-RESEARCH.md

# Phase 1 patterns to follow
@.planning/phases/01-core-integration/01-03-SUMMARY.md

<interfaces>
<!-- From DeerFlowClient in deerflow-harness -->
From deerflow/client.py (reference):
```python
class StreamEvent:
    type: str  # "values", "messages-tuple", "custom", "end"
    data: dict

# DeerFlowClient.stream() signature
def stream(self, message: str, thread_id: str | None = None) -> Iterator[StreamEvent]
```

<!-- From existing skill.py (to be modified in Plan 02) -->
From skill.py:
```python
def _get_deerflow_client() -> "type[DeerFlowClient]":
    # Deferred import with helpful error
```

<!-- From lib/modes.py -->
From lib/modes.py:
```python
def get_mode_config(mode: str = "standard") -> dict:
    # Returns DeerFlowClient kwargs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stream event handler module</name>
  <files>lib/stream.py, tests/test_stream.py</files>
  <behavior>
    - Test 1: stream_and_print yields tokens from messages-tuple events
    - Test 2: stream_and_print accumulates content by message id
    - Test 3: stream_and_print returns final accumulated text
    - Test 4: stream_and_print handles tool call events
    - Test 5: stream_and_print handles tool result events
    - Test 6: stream_and_print handles custom retry events
    - Test 7: stream_and_print handles end event
  </behavior>
  <action>
    Create lib/stream.py with:

    1. stream_and_print(client, message, thread_id) -> str function:
       - Iterates over client.stream(message, thread_id=thread_id)
       - For messages-tuple events with type="ai":
         - Print content delta immediately (flush=True)
         - Accumulate content by message id
         - Print tool call notification: "[Calling: {tool_name}]"
       - For messages-tuple events with type="tool":
         - Print completion: "[Tool {name} completed]"
       - For custom events with type="llm_retry":
         - Print retry progress: "[LLM retry {attempt}/{max}, waiting {wait}s]"
       - For end events: capture usage stats (optional logging)
       - Returns accumulated final text

    2. StreamEventHandler class (optional, for more complex use cases):
       - on_ai_message(msg_id, content, tool_calls)
       - on_tool_result(tool_name, result)
       - on_custom_event(event_type, data)
       - on_end(usage)

    Implementation notes:
    - Use `print(content, end="", flush=True)` for token streaming
    - Use `print(..., file=sys.stderr, flush=True)` for tool notifications
    - Accumulate chunks in dict by message id to handle parallel tool calls
    - Track last_id for final response assembly

    Reference RESEARCH.md Pattern 1 and Pattern 2 for exact event structure.
  </action>
  <verify>
    <automated>pytest tests/test_stream.py -v</automated>
  </verify>
  <done>All 7 streaming tests pass, stream.py implements event handling per spec</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mock fixtures for DeerFlowClient.stream()</name>
  <files>tests/conftest.py</files>
  <behavior>
    - Test fixture yields mock StreamEvent sequence
    - Mock can simulate AI messages, tool calls, tool results
    - Mock can simulate custom events (llm_retry)
    - Mock can simulate end event with usage stats
  </behavior>
  <action>
    Create tests/conftest.py with pytest fixtures:

    1. mock_stream_event factory fixture:
       - Creates StreamEvent-like objects with type and data
       - Can simulate all event types for testing

    2. mock_deerflow_client fixture:
       - Mocks DeerFlowClient.stream() method
       - Yields configurable sequence of events
       - Can be configured with events representing:
         - Simple text stream
         - Tool call sequence
         - Error scenarios
         - LLM retry events

    3. Helper fixtures for common scenarios:
       - simple_text_stream events
       - tool_call_stream events
       - retry_stream events

    Use unittest.mock.Mock for stream method, yielding list of mock events.
  </action>
  <verify>
    <automated>pytest tests/test_stream.py -v --fixtures</automated>
  </verify>
  <done>Fixtures available and used by test_stream.py tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Handle streaming errors gracefully</name>
  <files>lib/stream.py, tests/test_stream.py</files>
  <behavior>
    - Test 1: GeneratorExit is caught and re-raised (clean interrupt)
    - Test 2: Unknown exception during streaming is raised
    - Test 3: Partial output before error is preserved
  </behavior>
  <action>
    Extend lib/stream.py:

    1. Graceful error handling in stream_and_print:
       - Let GeneratorExit propagate (user interrupt)
       - Let other exceptions propagate (handled by caller)
       - Preserve accumulated output before error

    Note: Global error handling (GraphRecursionError, LLM errors) is in Plan 02.
    This task ensures stream_and_print does not silently swallow errors.

    Implementation:
    - No try/except in stream_and_print - let exceptions propagate naturally
    - Test verifies generator behavior when exceptions occur
  </action>
  <verify>
    <automated>pytest tests/test_stream.py -k test_error -v</automated>
  </verify>
  <done>Error handling tests pass, exceptions propagate correctly</done>
</task>

</tasks>

<verification>
- All tests pass: `pytest tests/test_stream.py -v`
- Coverage >= 80%: `pytest tests/test_stream.py --cov=lib/stream --cov-report=term-missing`
- Stream module can be imported: `python -c "from lib.stream import stream_and_print"`
</verification>

<success_criteria>
- [ ] lib/stream.py exists with stream_and_print function
- [ ] Token deltas print with flush=True for real-time output
- [ ] Tool call notifications appear during streaming
- [ ] Tool completion notifications appear
- [ ] LLM retry progress shown when retries occur
- [ ] End event handled without error
- [ ] All streaming tests pass
- [ ] Mock fixtures available in conftest.py
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-and-error-handling/02-01-SUMMARY.md`
</output>
