---
phase: 02-streaming-and-error-handling
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [skill.py, lib/errors.py, tests/test_skill.py, tests/test_errors.py]
autonomous: true
requirements: [ERRR-01, ERRR-02, ERRR-03, ERRR-04]
user_setup: []

must_haves:
  truths:
    - "User receives clear message when recursion limit is exceeded"
    - "User sees LLM provider errors with actionable guidance"
    - "User understands that sessions are stateless (no memory persistence)"
    - "Tool execution errors don't crash the skill"
  artifacts:
    - path: "skill.py"
      provides: "Entry point with streaming and error handling"
      modifies: "main_with_args to use stream_and_print with error wrapper"
    - path: "lib/errors.py"
      provides: "Extended error messages for streaming errors"
      contains: "STREAMING_ERRORS dict, format_streaming_error function"
  key_links:
    - from: "skill.py"
      to: "lib/stream.py"
      via: "from lib.stream import stream_and_print"
      pattern: "stream_and_print"
    - from: "skill.py"
      to: "lib/errors.py"
      via: "format_streaming_error"
      pattern: "GraphRecursionError"
---

<objective>
Add comprehensive error handling for streaming mode with actionable error messages.

Purpose: Users receive clear, actionable guidance when errors occur during agent runs, instead of cryptic stack traces.
Output: Modified skill.py with streaming and error handling, extended lib/errors.py with streaming error templates.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-and-error-handling/02-RESEARCH.md

# Phase 1 patterns
@.planning/phases/01-core-integration/01-02-SUMMARY.md

# Phase 2 plan 01 (dependency)
@.planning/phases/02-streaming-and-error-handling/02-01-PLAN.md

<interfaces>
<!-- From lib/stream.py (Plan 01) -->
From lib/stream.py:
```python
def stream_and_print(client: "DeerFlowClient", message: str, thread_id: str) -> str:
    """Stream agent response with real-time output."""
```

<!-- From existing skill.py -->
From skill.py:
```python
def main_with_args(argv: list[str]) -> None:
    """Main entry point with explicit args for testing."""
    # Currently uses client.chat(prompt) - will change to stream
```

<!-- From lib/errors.py -->
From lib/errors.py:
```python
def format_error(e: Exception) -> str:
    """Format an exception into an actionable error message."""
```

<!-- From langgraph.errors (external) -->
From langgraph.errors:
```python
class GraphRecursionError(Exception):
    """Raised when recursion limit is exceeded."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add streaming error templates to lib/errors.py</name>
  <files>lib/errors.py, tests/test_errors.py</files>
  <behavior>
    - Test 1: format_streaming_error formats GraphRecursionError
    - Test 2: format_streaming_error formats timeout errors
    - Test 3: format_streaming_error formats quota errors
    - Test 4: format_streaming_error formats auth errors
    - Test 5: STATELESS_SESSION_INFO message exists
  </behavior>
  <action>
    Extend lib/errors.py with:

    1. STREAMING_ERRORS dict with templates for:
       - "recursion": GraphRecursionError message with actionable guidance
         (suggest simplifying, using --pro mode, checking for loops)
       - "llm_timeout": API timeout with guidance to wait or simplify
       - "llm_quota": Quota exceeded with provider dashboard links
         (OpenAI: platform.openai.com/account/usage)
         (Anthropic: console.anthropic.com/settings/usage)
       - "llm_auth": Authentication failed with API key guidance

    2. format_streaming_error(e: Exception) -> str function:
       - Catches GraphRecursionError -> returns recursion message
       - Detects timeout keywords -> returns llm_timeout message
       - Detects quota keywords -> returns llm_quota message
       - Detects auth keywords (401, unauthorized) -> returns llm_auth message
       - Falls back to raw error message

    3. STATELESS_SESSION_INFO constant:
       - Documents stateless behavior
       - Explains no memory persistence
       - Notes thread_id is for file isolation only
       - Suggests deer-flow server for persistent threads

    Reference RESEARCH.md Pattern 3 and Pattern 4 for exact message content.
  </action>
  <verify>
    <automated>pytest tests/test_errors.py -k test_streaming -v</automated>
  </verify>
  <done>All streaming error tests pass, error templates defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify skill.py to use streaming with error handling</name>
  <files>skill.py, tests/test_skill.py</files>
  <behavior>
    - Test 1: main_with_args uses stream_and_print instead of chat
    - Test 2: GraphRecursionError caught and shows clear message
    - Test 3: KeyboardInterrupt caught and shows interrupt message
    - Test 4: Generic exceptions formatted with format_streaming_error
    - Test 5: Exit code is 1 on error, 130 on interrupt
  </behavior>
  <action>
    Modify skill.py:

    1. Add import:
       from lib.stream import stream_and_print
       from lib.errors import format_streaming_error
       from langgraph.errors import GraphRecursionError

    2. Create stream_with_error_handling wrapper:
       def stream_with_error_handling(client, prompt: str, thread_id: str) -> str:
           try:
               return stream_and_print(client, prompt, thread_id)
           except GraphRecursionError:
               print(STREAMING_ERRORS["recursion"], file=sys.stderr)
               sys.exit(1)
           except KeyboardInterrupt:
               print("\n[Interrupted]", file=sys.stderr)
               sys.exit(130)
           except Exception as e:
               print(format_streaming_error(e), file=sys.stderr)
               sys.exit(1)

    3. Modify main_with_args:
       - Generate thread_id: str(uuid.uuid4())
       - Replace client.chat(prompt) with stream_with_error_handling
       - Print newline after streaming completes

    4. Note: The deferred import pattern for DeerFlowClient stays the same.

    Implementation notes:
    - Thread IDs are UUIDs for stateless sessions
    - Exit codes: 1 for errors, 130 for SIGINT (128 + 2)
    - Final newline ensures clean terminal output
  </action>
  <verify>
    <automated>pytest tests/test_skill.py -v</automated>
  </verify>
  <done>skill.py uses streaming, all error handling tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Update tests with streaming mock fixtures</name>
  <files>tests/test_skill.py</files>
  <action>
    Extend tests/test_skill.py to use streaming mocks:

    1. Update existing tests for streaming mode:
       - Tests should use mock for DeerFlowClient.stream() not chat()
       - Verify stream_and_print is called

    2. Add error handling tests:
       - test_recursion_limit_shows_guidance: Mock stream to raise GraphRecursionError
       - test_keyboard_interrupt_exit_code: Mock stream to raise KeyboardInterrupt
       - test_generic_error_formatted: Mock stream to raise generic Exception

    3. Integration-style test:
       - Mock entire stream flow with events
       - Verify main_with_args completes with correct output

    Use conftest.py mock_deerflow_client fixture (from Plan 01).
  </action>
  <verify>
    <automated>pytest tests/test_skill.py -v --cov=skill</automated>
  </verify>
  <done>All skill tests updated for streaming, error handling tests pass</done>
</task>

</tasks>

<verification>
- All tests pass: `pytest tests/ -v`
- Coverage >= 80%: `pytest tests/ --cov=lib --cov-report=term-missing`
- Streaming works: Manual invocation shows token-by-token output
- Error handling works: Manual invocation with recursion shows clear message
</verification>

<success_criteria>
- [ ] skill.py uses stream_and_print instead of chat()
- [ ] GraphRecursionError caught with clear message
- [ ] KeyboardInterrupt handled with exit code 130
- [ ] LLM timeout/quota/auth errors formatted with guidance
- [ ] STATELESS_SESSION_INFO constant defined
- [ ] All error handling tests pass
- [ ] Exit codes correct (1 for error, 130 for interrupt)
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-and-error-handling/02-02-SUMMARY.md`
</output>
